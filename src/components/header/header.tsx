import { Logo } from './components/logo';

export const Header = () => {
  return (
    <header className="bg-slate-700 text-white p-4 flex justify-center items-center">
      <Logo />
    </header>
  );
};
